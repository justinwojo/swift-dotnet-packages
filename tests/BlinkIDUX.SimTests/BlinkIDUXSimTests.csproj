<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-ios</TargetFramework>
    <RuntimeIdentifier Condition="'$(RuntimeIdentifier)' == ''">iossimulator-arm64</RuntimeIdentifier>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ApplicationId>com.swiftbindings.blinkiduxsimtests</ApplicationId>
    <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
    <ApplicationVersion>1</ApplicationVersion>
    <SupportedOSPlatformVersion>15.0</SupportedOSPlatformVersion>
    <IncludeSwiftBindingsRuntimeNative>true</IncludeSwiftBindingsRuntimeNative>
  </PropertyGroup>

  <ItemGroup>
    <None Include="Info.plist" />
  </ItemGroup>

  <!-- Reference both BlinkID and BlinkIDUX library projects -->
  <ItemGroup>
    <ProjectReference Include="../../libraries/BlinkID/SwiftBindings.BlinkID.csproj" />
    <ProjectReference Include="../../libraries/BlinkIDUX/SwiftBindings.BlinkIDUX.csproj" />
  </ItemGroup>

  <!-- Native frameworks must be referenced directly (NativeReference doesn't propagate through ProjectReference) -->
  <ItemGroup>
    <NativeReference Include="../../libraries/BlinkID/BlinkID.xcframework">
      <Kind>Framework</Kind>
    </NativeReference>
    <NativeReference Include="../../libraries/BlinkIDUX/BlinkIDUX.xcframework">
      <Kind>Framework</Kind>
    </NativeReference>
  </ItemGroup>

  <!-- Wrapper xcframework: must be in a Target (not static ItemGroup) so the Exists() check
       runs AFTER the library project builds. Static ItemGroups evaluate before any build targets,
       so on a clean build the wrapper doesn't exist yet and gets silently omitted. -->
  <Target Name="_AddSwiftWrapperReference"
          BeforeTargets="ResolveNativeReferences"
          DependsOnTargets="ResolveProjectReferences">
    <ItemGroup>
      <NativeReference Include="../../libraries/BlinkID/obj/Debug/net10.0-ios/swift-binding/BlinkIDSwiftBindings.xcframework"
                       Condition="Exists('../../libraries/BlinkID/obj/Debug/net10.0-ios/swift-binding/BlinkIDSwiftBindings.xcframework')">
        <Kind>Framework</Kind>
      </NativeReference>
      <NativeReference Include="../../libraries/BlinkIDUX/obj/Debug/net10.0-ios/swift-binding/BlinkIDUXSwiftBindings.xcframework"
                       Condition="Exists('../../libraries/BlinkIDUX/obj/Debug/net10.0-ios/swift-binding/BlinkIDUXSwiftBindings.xcframework')">
        <Kind>Framework</Kind>
      </NativeReference>
    </ItemGroup>
  </Target>

  <!-- Copy SPM resource bundle stubs into the app so resource_bundle_accessor.swift can find them at runtime -->
  <Target Name="_CopySwiftResourceBundleStubs"
          AfterTargets="_CreateAppBundle"
          DependsOnTargets="ResolveProjectReferences"
          Condition="Exists('../../libraries/BlinkIDUX/obj/Debug/net10.0-ios/swift-binding/_resource-bundles.txt')">
    <ReadLinesFromFile File="../../libraries/BlinkIDUX/obj/Debug/net10.0-ios/swift-binding/_resource-bundles.txt">
      <Output TaskParameter="Lines" ItemName="_BlinkIDUXResourceBundleName" />
    </ReadLinesFromFile>
    <MakeDir Directories="$(AppBundleDir)/%(_BlinkIDUXResourceBundleName.Identity).bundle"
             Condition="'%(_BlinkIDUXResourceBundleName.Identity)' != ''" />
  </Target>

</Project>
