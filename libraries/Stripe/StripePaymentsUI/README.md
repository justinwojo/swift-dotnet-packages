# SwiftBindings.Stripe.PaymentsUI

Native Swift interop bindings for `StripePaymentsUI` — the UI controls layered on top of [`StripePayments`](https://www.nuget.org/packages/SwiftBindings.Stripe.Payments). Includes `STPCardFormView`, `STPPaymentCardTextField`, and the supporting view-styling primitives, for apps that want to compose their own payment-entry UI rather than use [PaymentSheet](https://www.nuget.org/packages/SwiftBindings.Stripe.PaymentSheet). These are not Objective-C proxy wrappers — they use .NET 10's native Swift interop for direct, high-performance calls into Swift APIs from C#.

## Installation

```
dotnet add package SwiftBindings.Stripe.PaymentsUI
```

## Requirements

- .NET 10.0+
- iOS 15.0+
- macOS host for development

## Usage

### Card form view (full address + card)

```csharp
using StripePaymentsUI;

var form = new STPCardFormView(STPCardFormViewStyle.Borderless);
// add `form` to your UIKit view hierarchy
```

### Card text field (single-line)

```csharp
var textField = new STPPaymentCardTextField(new Swift.CGRect(0, 0, 320, 44));
```

## Documentation

- [Card element guides](https://docs.stripe.com/payments/accept-a-payment?platform=ios&ui=elements)
- [`StripePaymentsUI` source](https://github.com/stripe/stripe-ios/tree/master/StripePaymentsUI)

## How It Works

These bindings are auto-generated by [Swift Dotnet Bindings](https://github.com/justinwojo/swift-dotnet-bindings) using .NET 10's native Swift interop via the [SwiftBindings SDK](https://www.nuget.org/packages/SwiftBindings.Sdk).

## License

The bindings are MIT licensed. The Stripe iOS SDK is MIT licensed — see [Stripe's license](https://github.com/stripe/stripe-ios/blob/master/LICENSE).
