# SwiftBindings.Stripe.PaymentSheet

Native Swift interop bindings for `StripePaymentSheet` — Stripe's drop-in payment UI for the [iOS SDK](https://github.com/stripe/stripe-ios). Renders all enabled payment methods, handles 3D Secure, Apple Pay, and saved-card flows, and returns a result your app can act on. Pair with `CustomerSheet` to manage saved payment methods. These are not Objective-C proxy wrappers — they use .NET 10's native Swift interop for direct, high-performance calls into Swift APIs from C#.

## Installation

```
dotnet add package SwiftBindings.Stripe.PaymentSheet
```

## Requirements

- .NET 10.0+
- iOS 15.0+
- macOS host for development

## Usage

### Customise appearance

```csharp
using StripePaymentSheet;

var appearance = PaymentSheet.Appearance.Default;
appearance.CornerRadius = 12.0;
appearance.BorderWidth = 2.0;
```

### Configure and present

```csharp
// Construct PaymentSheet with a PaymentIntent client secret produced by your backend.
// var sheet = new PaymentSheet(paymentIntentClientSecret, configuration);
// sheet.Present(from: viewController, completion: result => { ... });
```

### Address collection

```csharp
var addressType = new AddressViewController.AddressDetails.AddressType(
    city: "San Francisco",
    country: "US",
    line1: "123 Market St",
    line2: null,
    postalCode: "94107",
    state: "CA");
var details = new AddressViewController.AddressDetails(addressType, name: "Test User");
```

## Documentation

- [Accept a payment with PaymentSheet](https://docs.stripe.com/payments/accept-a-payment?platform=ios)
- [PaymentSheet appearance API](https://docs.stripe.com/elements/appearance-api?platform=ios)
- [`StripePaymentSheet` source](https://github.com/stripe/stripe-ios/tree/master/StripePaymentSheet)

## How It Works

These bindings are auto-generated by [Swift Dotnet Bindings](https://github.com/justinwojo/swift-dotnet-bindings) using .NET 10's native Swift interop via the [SwiftBindings SDK](https://www.nuget.org/packages/SwiftBindings.Sdk).

## License

The bindings are MIT licensed. The Stripe iOS SDK is MIT licensed — see [Stripe's license](https://github.com/stripe/stripe-ios/blob/master/LICENSE).
