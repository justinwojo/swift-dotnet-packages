# SwiftBindings.Stripe.CardScan

Native Swift interop bindings for `StripeCardScan` — the on-device card-scanning module of the [Stripe iOS SDK](https://github.com/stripe/stripe-ios). Presents a camera sheet that recognises the PAN of a physical card and returns the result without sending images off-device. These are not Objective-C proxy wrappers — they use .NET 10's native Swift interop for direct, high-performance calls into Swift APIs from C#.

## Installation

```
dotnet add package SwiftBindings.Stripe.CardScan
```

## Requirements

- .NET 10.0+
- iOS 15.0+
- macOS host for development
- Camera usage description in `Info.plist`

## Usage

```csharp
using StripeCardScan;

var sheet = new CardScanSheet();
// sheet.Present(from: viewController, completion: result => { ... });
```

## Documentation

- [Card scanning on iOS](https://docs.stripe.com/card-scan)
- [`StripeCardScan` source](https://github.com/stripe/stripe-ios/tree/master/StripeCardScan)

## How It Works

These bindings are auto-generated by [Swift Dotnet Bindings](https://github.com/justinwojo/swift-dotnet-bindings) using .NET 10's native Swift interop via the [SwiftBindings SDK](https://www.nuget.org/packages/SwiftBindings.Sdk).

## License

The bindings are MIT licensed. The Stripe iOS SDK is MIT licensed — see [Stripe's license](https://github.com/stripe/stripe-ios/blob/master/LICENSE).
