<Project Sdk="SwiftBindings.Sdk/0.9.0">

  <PropertyGroup>
    <TargetFramework>net10.0-ios</TargetFramework>

    <!-- NuGet package metadata -->
    <PackageId>SwiftBindings.Stripe.Core</PackageId>
    <Version>25.11.0</Version>
    <Description>.NET bindings for the StripeCore Swift library</Description>
    <PackageTags>swift;dotnet;interop;stripecore;ios</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <!-- Package the README -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

</Project>
