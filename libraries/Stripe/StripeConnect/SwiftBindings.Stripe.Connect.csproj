<Project Sdk="SwiftBindings.Sdk/0.1.1">

  <PropertyGroup>
    <TargetFramework>net10.0-ios</TargetFramework>

    <!-- NuGet package metadata -->
    <PackageId>SwiftBindings.Stripe.Connect</PackageId>
    <Version>25.6.2</Version>
    <Description>.NET bindings for the StripeConnect Swift library</Description>
    <PackageTags>swift;dotnet;interop;stripeconnect;ios</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <!-- Package the README -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

<!-- BEGIN auto-detected SwiftFrameworkDependency -->
  <ItemGroup>
    <SwiftFrameworkDependency Include="../StripeCore/StripeCore.xcframework" />
    <SwiftFrameworkDependency Include="../StripeFinancialConnections/StripeFinancialConnections.xcframework" />
    <SwiftFrameworkDependency Include="../StripeUICore/StripeUICore.xcframework" />
  </ItemGroup>
  <!-- END auto-detected SwiftFrameworkDependency -->

  <ItemGroup>
    <ProjectReference Include="../StripeCore/SwiftBindings.Stripe.Core.csproj" />
  </ItemGroup>

</Project>
