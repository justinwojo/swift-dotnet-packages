<Project Sdk="SwiftBindings.Sdk/0.5.0">

  <PropertyGroup>
    <TargetFramework>net10.0-ios</TargetFramework>

    <!-- NuGet package metadata -->
    <PackageId>SwiftBindings.Kingfisher</PackageId>
    <Version>1.0.0</Version>
    <Description>Native Swift interop bindings for the Kingfisher image downloading and caching library</Description>
    <PackageTags>swift;dotnet;interop;kingfisher;image;cache;download;ios</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <!-- Package the README -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

</Project>
